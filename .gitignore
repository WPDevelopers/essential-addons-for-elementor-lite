.sass-cache
.DS_Store
.phpintel
./gulpfile.js
pnpm-lock.yaml
# root package-lock.json is committed (CI uses npm ci); nested app locks are not
*/**/package-lock.json
composer.lock
/node_modules
.idea/*
/tools/vendor/
/tools/composer.lock

# E2E testing
test-results/
playwright-report/

# Angie MCP bundle build artifact (baseline lives in bin/angie-bundle.allowed.json)
.angie-bundle-meta.json
